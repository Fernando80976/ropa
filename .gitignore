.venv/
.env
# ropa.db SI se versiona (via Git LFS, ver .gitattributes): es la unica forma
# de que el servidor tenga el catalogo indexado sin reconstruirlo en cada
# despliegue. Es una excepcion a la regla de CLAUDE.md, tomada al desplegar.
# Las BD de usar y tirar que se creen al lado si se ignoran.
ropa_*.db
*.db-journal
*.db-wal
articles.csv
__pycache__/
# Modelo de sentence-transformers descargado por HuggingFace
.cache/

# Modelo de embeddings: 113 MB. Lo descarga preparar_modelo.py.
modelo_onnx/
# Referencia para comparar fp32 con int8, se regenera.
referencia_fp32.json
