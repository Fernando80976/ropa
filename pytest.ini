[pytest]
markers =
    integracion: llama a servicios externos (Groq). Necesita GROQ_API_KEY.
# Por defecto no se ejecutan: dependen de la red y de la cuota de la API.
# Para lanzarlos: pytest -q -m integracion
addopts = -m "not integracion"
